---

- name: template config deployment true
  command: bundle config deployment 'true'
  args:
    chdir: /var/www/mastodon

- name: template config without developement test
  command: bundle config without 'development test'
  args:
    chdir: /var/www/mastodon